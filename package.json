{
  "name": "neomokdeul",
  "version": "0.1.0",
  "private": true,
  "description": "너의 목소리가 들려 (Socially) — 8일간 익명 전화 소울메이트 매칭 서비스",
  "scripts": {
    "dev:landing": "pnpm --filter landing dev",
    "dev:admin": "pnpm --filter admin dev",
    "dev:webapp": "pnpm --filter webapp dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.33.0"
}
